{
  "name": "athar",
  "version": "1.0.0",
  "description": "أثر — Digital Experience for Micro-Acts of Kindness",
  "scripts": {
    "build": "mkdir -p dist && cp -r index.html css js dist/",
    "start": "npx serve dist"
  }
}
